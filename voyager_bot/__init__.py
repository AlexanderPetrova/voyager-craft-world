# modules/__init__.py
# The presence of this file makes Python treat the `modules`
# directory as a package, allowing for imports like `from modules import ...`.
# It can be left empty to avoid circular import issues.
